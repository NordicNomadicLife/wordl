{
    "name": "wordl-root",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix client\" \"npm start --prefix backend\"",
        "build": "npm install --prefix client && npm run build --prefix client"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
